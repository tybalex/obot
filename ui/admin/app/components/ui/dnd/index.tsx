import * as Primitive from "@dnd-kit/core";

export const DndContext = Primitive.DndContext;

export type DragEndEvent = Primitive.DragEndEvent;

export const useDndContext = Primitive.useDndContext;
