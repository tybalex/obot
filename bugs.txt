SERVER_URL is just weird
If the user doesn't reply to prompt, sys.prompt caller doesn't get an error
used ../code in the slack read tool
change LLMRequest/Response to EngineRequest/Response capture the chatresponsecached field
capture USAGE field
